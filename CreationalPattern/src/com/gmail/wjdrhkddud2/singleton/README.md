## Singleton 패턴

### 설명

- 인스턴스를 하나로만 유지하고 싶을 떄 사용한다.
- 현실세계 예시로는 여러 사용자가 프린터를 하나만 이용하는 것으로 비유할 수 있고
- 코드 사례로는 파이어베이스의 안드로이드 SDK에서 아주 흔히 사용된다.
- new 키워드를 사용하지 않고 Object.getInstance().method() 처럼 한줄로 처리할 수있고,
- SOLID 원칙에 위배될 여지는 찾지 못 했다.